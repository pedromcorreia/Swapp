@{
    ViewBag.Title = "Cadastro realizado com sucesso"; 
	Layout = "~/Views/Shared/_Layout_LoginCadastro.cshtml";
}

<main>
    <section id="cadastro">
        <div class="container">
            <div class="box-principal">
                <center><h1><img src="../Content/img/acesso/logo.png" alt="Bcredi"></h1></center>
                <p class="text-center cadastro-text mb-30">Cadastro realizado <span class="destaque">com sucesso!</span></p>
                <div class="conteudo-texto text-center">
                    <p>Um email de confirmação foi enviado para <span class="cor-link-azul">@ViewBag.Email</span>. Siga as instruções recebidas para ativar sua conta.</p>
                    <p>Caso não tenha recebido o link, verifique sua caixa de SPAM. Se ainda assim o email não chegar, solicite o <a id="reenvioLink" href="" class="cor-link-azul ">reenvio do link</a>.</p>
                </div>
                <a href="https://www.bcredi.com.br/" class="cor-link-amarelo text-uppercase"><i class="fa fa-chevron-circle-left" aria-hidden="true"></i>Voltar ao site</a>
                <hr>
                <div class="area-suporte">
                    <p class="text-center inicial mb-30">Está com dúvidas? Entre em contato pelo chat ou envie um email.</p>
                    <div class="area-botoes">
                        <div class="btn-chat"><a href="javascript:void($zopim.livechat.window.show())" class="valign-center btnChat tags-ga" data-categoria="header" data-acao="clique"><img src="../Content/img/acesso/btn-chat.png"></a></div>
                        <div class="btn-email"><a href="mailto:contato@bcredi.com.br"><img src="../Content/img/acesso/btn-email.png"></a></div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>

<div id="aviso-envio" class="modal fade" role="dialog">
    <div class="modal-dialog margin-auto">
        <div class="modal-content">
            <div class="modal-body text-center">
                <h2>Email enviado <span class="destaque">com sucesso</span>!</h2>
                <p>Verifique em seu email o link para ativar sua conta.</p>
                <button type="button" class="btn btn-medio btn-cinza mt-30" data-dismiss="modal">Fechar</button>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">

    $(document).ready(function () {

        //## Enable e Disable Input via Radio ######
        $('#reenvioLink').click(function () {

            //alert('aqui');

            $.ajax({
                url: "/Usuario/EmailExiste",
                type: "POST",
                dataType: "json",
                data: {
                    //imovelJSON: imovelJSON
                },
                success: function (dados) {
                    ////var data = dados.Data;
                    //////Lista de rendas do usuario
                    //var result = $.parseJSON(dados.Data);

                    //if (result.Data) {
                    //    $(".alert-success").fadeIn(1000);
                    //    $(".alert-danger").addClass("hide");
                    //    $(".alert-success").removeClass("hide");
                    //    $(".alert-success").delay(5000).fadeOut(2000);
                    //}
                },
                error: function (result) {
                    $(".alert-danger").fadeIn(1000);
                    $(".alert-danger").removeClass("hide");
                    $(".alert-danger").delay(5000).fadeOut(2000);
                }
            })
            event.preventDefault();
        });

    });

</script>